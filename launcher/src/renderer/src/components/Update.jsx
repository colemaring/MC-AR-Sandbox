import React, { useEffect, useRef } from 'react'
import Button from 'react-bootstrap/esm/Button'

// grab newest plugin jar from https://github.com/colemaring/MC-AR-Sandbox/tree/main/launcher/server/plugins
// if current version outdated or not existing.
// update should stop minecraft server, replace jar, and restart server.

function Update() {
  return <Button>Update</Button>
}

export default Update
